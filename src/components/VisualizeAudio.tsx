export interface VisualizeAudioProps {
    isDisplayed: boolean;
    isRecording: boolean;
}

export function VisualizeAudioProps(Props: VisualizeAudioProps) {
    const isDisplayed = Props.isDisplayed;
    const isRecording = Props.isRecording;


}